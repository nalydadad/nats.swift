# Minimal nats-server config with a plaintext WebSocket gateway on :8080.
# Used by realworld-test.sh for the wss/ws + proxy smoke test.
#
# The client URL is then:  ws://127.0.0.1:8080
port: 4222

websocket {
  port: 8080
  no_tls: true
}
